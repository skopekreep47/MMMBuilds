@model MechListViewModel


<h1>@Model.CurrentCategory</h1>

<div class="row row-cols-1 row-cols-md-3 g-4">

    @foreach (var mech in Model.Mechs)
    {
        <partial name="_MechCard" model="mech" />
    }

</div>